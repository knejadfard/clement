PROJECT_NAME = "PROJECT_C"
PROJECT_NUMBER = 1
OUTPUT_DIRECTORY = .
RECURSIVE = YES

INPUT = ${DOXYGEN_INCLUDES}

# EXAMPLE_PATH = ./examples
# EXAMPLE_PATTERNS = *.hpp *.cpp

CLANG_ASSISTED_PARSING = YES
CLANG_OPTIONS = -std=c++11
CLANG_OPTIONS += -stdlib=libc++
CLANG_OPTIONS += -I${LLVM_ROOT}/include/c++/v1/
CLANG_OPTIONS += -I${LLVM_ROOT}/lib/clang/8.0.0/include/
CLANG_OPTIONS += -I${BOOST_ROOT}/include/
